apiVersion: v1
kind: Service
metadata:
  name: feedback-backend-service
  namespace: backend
  annotations:
    external-dns.alpha.kubernetes.io/hostname: feedback-backend.dev-k8.livingthings.co.in
spec:
  type: NodePort
